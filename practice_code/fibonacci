

def fibonacci_sequence_printer(n):
    a, b = 0, 1
    for _ in range(n):
        print(a)
        a, b = b, a + b

# Example usage:
fibonacci_sequence_printer = int(input("Pick a number: "))
print(n)